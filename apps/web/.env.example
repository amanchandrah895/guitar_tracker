# Database — leave DATABASE_URL unset; app uses SQLite automatically
# DATA_DIR=/data   ← set this on Render to your mounted disk path

# Admin login password (change before deploying!)
ADMIN_PASSWORD=change_me_before_deploying
